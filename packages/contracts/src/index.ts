// Shared contracts package: Zod schemas + derived types for every shape that
// crosses the network between apps/api and apps/web.
//
// This is a Phase 0 placeholder. The real schemas (enums, interview, session,
// transcript, feedback, progress, errors) land in Phase 1 per shared.md.

export const CONTRACTS_PACKAGE_NAME = '@coach/contracts';
